# refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S%.3f%z)} {h({l})} [{T}] {M} : {m}{n}"
  # requests:
  #   kind: file
  #   path: "log/requests.log"
  #   encoder:
  #     pattern: "{d(%Y-%m-%d %H:%M:%S%.3f%z)} {l} [{M}] - {m}{n}"

root:
  level: debug
  appenders:
    - stdout

# loggers:
#   app::backend::db:
#     level: info

#   app::requests:
#     level: info
#     appenders:
#       - requests
#     additive: false